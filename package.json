{
  "name": "novum-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ novum-test",
    "create-local": "graph create --node http://localhost:8020/ novum-test",
    "remove-local": "graph remove --node http://localhost:8020/ novum-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 novum-test",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.69.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
