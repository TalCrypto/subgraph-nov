
type UserBalance @entity {
  id: Bytes! # address
  balance: BigInt!
}

type UserPoint @entity {
  id: Bytes! # address
  point: BigInt!
  lastUpdatedTimestamp: BigInt!
  referer: Referral
}

type Referral @entity {
  id: Bytes! # address
  referees: [UserPoint!] @derivedFrom(field: "referer")
}

